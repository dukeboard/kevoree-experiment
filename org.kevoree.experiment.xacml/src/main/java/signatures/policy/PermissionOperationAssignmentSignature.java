/*******************************************************************************
 * Pattern signature of the policy.PermissionOperationAssignment pattern
 * Generated by EMF-IncQuery
 *******************************************************************************/

package signatures.policy;

import java.util.Arrays;

import org.eclipse.emf.ecore.EObject;
import org.eclipse.emf.ecore.EStructuralFeature;
import org.eclipse.viatra2.emf.incquery.runtime.api.IPatternSignature;
import org.eclipse.viatra2.emf.incquery.runtime.api.impl.BasePatternSignature;

/**
 * Pattern-specific signature object of the policy.PermissionOperationAssignment pattern.
 * Class fields correspond to parameters of the pattern. Fields with value null are considered unassigned.
 * Each instance is a (possibly partial) substitution of pattern parameters, 
 * usable e.g. to represent a match of the pattern in the result of a query, 
 * or to specify the bound (fixed) input parameters when issuing a query.
 */
@SuppressWarnings("unused")
public final class PermissionOperationAssignmentSignature extends BasePatternSignature implements IPatternSignature {

	// constructor
	public PermissionOperationAssignmentSignature(Object PERM, Object OPE) {
		this.fPERM = PERM;
		this.fOPE = OPE;	
	}

	//private attributes
	private Object fPERM;
	private Object fOPE;
	
	// getter methods
	/** Returns the value of the parameter with the given name, or null if name is invalid. */
	@Override
	public Object get(String parameterName) {
		if ("PERM".equals(parameterName)) return fPERM;
		if ("OPE".equals(parameterName)) return fOPE;
		return null;
	}	
	
	public Object getValueOfPERM(){
		 return fPERM;
	}
	public Object getValueOfOPE(){
		 return fOPE;
	}	
	
	// setter methods
	/** 
	 * Sets the parameter with the given name to the given value. 
	 * @returns true if successful, false if parameter name is invalid. May also fail and return false if the value type is incompatible. 
	 */
	@Override
	public boolean set(String parameterName, Object newValue) {
		if ("PERM".equals(parameterName)) {
			fPERM = newValue;
			return true;
		}
		if ("OPE".equals(parameterName)) {
			fOPE = newValue;
			return true;
		}
		return false;
	}
	
	public void setValueOfPERM(Object PERM){
		 this.fPERM=PERM;
	}
	public void setValueOfOPE(Object OPE){
		 this.fOPE=OPE;
	}

	// overridden prettyPrint(), hashCode(), equals() with Tuple-semantics
	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (! (obj instanceof IPatternSignature))
			return false;
		IPatternSignature otherSig = (IPatternSignature) obj;
		if (!patternName().equals(otherSig.patternName()))
			return false;
		if (!PermissionOperationAssignmentSignature.class.equals(obj.getClass()))
			return Arrays.deepEquals(toArray(), otherSig.toArray());
		PermissionOperationAssignmentSignature other = (PermissionOperationAssignmentSignature) obj;
		if (fPERM == null) {if (other.fPERM != null) return false;}
		else if (!fPERM.equals(other.fPERM)) return false;
		if (fOPE == null) {if (other.fOPE != null) return false;}
		else if (!fOPE.equals(other.fOPE)) return false;
		return true;
	}
	
	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + ((fPERM == null) ? 0 : fPERM.hashCode());
		result = prime * result + ((fOPE == null) ? 0 : fOPE.hashCode());
		return result;
	}

	@Override
	public String prettyPrint() {
		StringBuilder result = new StringBuilder();
		result.append("\"PERM\"=" + prettyPrintValue(fPERM));
		result.append(", \"OPE\"=" + prettyPrintValue(fOPE));
		return result.toString();
	}

	// conversion and reflection
	/** Converts the signature to an array representation, with each pattern parameter at their respective position */
	@Override
	public Object[] toArray() {
		return new Object[] {fPERM, fOPE};
	}
	
	/** Identifies the name of the pattern for which this is a signature. */
	@Override
	public String patternName() {
		return "policy.PermissionOperationAssignment";
	}
	
	/** Returns the list of symbolic parameter names. */
	@Override
	public String[] parameterNames() {
		return parameterNames;
	}
	private static String[] parameterNames = {"PERM", "OPE"};
	
	
}
